#include<stdio.h>

int main(void) // Void - unsure datatype
{
    int x=10;//int x = 1 0 - TOKENS
    printf("%d",x);
    return 0;
}
/*A token is defined as the smallest indivisible component of a statement
without which , the statement will not work as desired.*/
// Spaces are also tokens
// NULL-Not Available to the program